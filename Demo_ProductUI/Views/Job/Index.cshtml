
@using EntityLayer.Concrete
@model List<Job>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_UILayout.cshtml";
}

<table class="table table-bordered">
    <tr>
        <th>#</th>
        <th>Meslek Adı</th>
        <th>Sil</th>
        <th>Güncelle</th>
        <th>Detay</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Name</td>
            <td> <a href="/Job/DeleteJob/@item.Id" class="btn btn btn-outline-danger" />Sil</td>
            <td> <a href="/Job/UpdateJob/@item.Id" class="btn btn btn-outline-warning" />Düzenle</td>
            <td> <a href="/Job/DetailJob/@item.Id" class="btn btn btn-outline-info" />Detaylar</td>
        </tr>
    }
</table>

